# Opens the vanilla-private ResourcePackRepository$Entry(ResourcePackRepository, File)
# constructor so built-in packs can create repository entries with plain code instead of
# reflection. This is the real constructor behind the source-level private Entry(File)
# (the javac access bridge next to it carries a synthetic dummy parameter whose type
# differs between the MCP recompile and the obfuscated production jar).
#
# Applied twice by ForgeGradle 1.2 (deobfBinJar/deobfuscateJar, via the *_at.cfg scan of
# main resources) for the development compile, and at runtime by FML's ModAccessTransformer
# for the production jar.
public net.minecraft.client.resources.ResourcePackRepository$Entry <init>(Lnet/minecraft/client/resources/ResourcePackRepository;Ljava/io/File;)V
